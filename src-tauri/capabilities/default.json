{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core commands and events only. Every app feature is a custom command.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
