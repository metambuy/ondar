{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The popover (the only window): core commands and events only. App-defined commands need no ACL entry from a local page; every app feature is one of those.",
  "windows": ["panel"],
  "permissions": ["core:default"]
}
