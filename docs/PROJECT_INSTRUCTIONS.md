# Ondar — Project Instructions (stub)

This file used to hold the text pasted into the **Project instructions** field of Ondar's
Claude Project. That text now lives in the Project's instructions field itself, not here —
this file is kept only as a pointer, so it stops drifting as a fourth copy of the project
description.

**ONDAR.md** is the source of truth for decisions, verified versions, and milestone numbering.
**`docs/BUILD_PLAN.md`** carries per-milestone exit criteria and open questions. If you need
the Project-instructions text itself, read it from the Claude Project, not from here.
