# ts-rs writes generated TypeScript here when `cargo test` runs in the workspace.
# `relative = true` resolves against this file's parent directory (the repo root).
[env]
TS_RS_EXPORT_DIR = { value = "src/bindings", relative = true }
